// Root layout yang minimal — pola standar next-intl App Router.
// Tag <html> dan <body> ada di app/[locale]/layout.tsx
export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
